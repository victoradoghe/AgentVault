// Stub for Next.js's `server-only` guard module, which throws when imported
// outside a server bundle. Modules under test import it for its side effect
// only, so an empty module is a faithful stand-in.
export {};
